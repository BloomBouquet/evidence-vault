# Migration PR scope

This PR migrates the legacy Evidence Vault baseline into the dedicated BloomBouquet repository. It does not claim the MVP is complete and it does not implement new product behavior beyond repository CI/provenance needed to verify the migration.
